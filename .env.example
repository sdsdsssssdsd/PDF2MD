# Example environment variables (copy to .env and fill locally).
# Never commit real values.

# Optional DeepSeek API key for future reasoning repair (not used in Alpha)
DEEPSEEK_API_KEY=

# Optional Hugging Face / MinerU source overrides
# HF_ENDPOINT=
# MINERU_MODEL_SOURCE=
# PDF2MD_PYTHON=
# PDF2MD_DOCLING_ARTIFACTS=
